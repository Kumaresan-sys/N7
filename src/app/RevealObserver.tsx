"use client";

/**
 * Replaced by GSAP ScrollTrigger fallback in ScrollAnimations.tsx
 * Kept empty to avoid breaking existing imports before layout.tsx is updated.
 */
export default function RevealObserver() {
  return null;
}
